# 使用指南

# 前置说明
本使用指南将详细介绍如何使用本 Skill。`doc`、`drawio`、`playwright-cli` 需要按任务类型选用，不是每次都必须同时使用。

前置环境：

- 如果只做规则解释、章节写作、常规文档整理，可以只使用本 Skill 或按需搭配其他 Skill。
- 如果需要处理 `.docx` 排版、分节、页眉页脚和最终 Word 检查，使用 `doc`。
- 如果需要正式图表，使用 `drawio`。
- 如果需要浏览器截图、页面证据或 UI 流程验证，使用 `playwright-cli`。
- 如果要运行 `build_reference_crossrefs.py`、`build_figure_crossrefs.py`、`build_table_crossrefs.py`，必须满足 `Windows + Microsoft Word + pywin32`。

最小 Python 依赖安装命令：

```bash
py -m pip install python-docx pywin32
```

## 1. 各技能的作用说明  
- **doc skill**: 主要用于处理文档生成、转换与管理，可以将各类文档整合成一个输出。  
- **drawio skill**: 用于创建和编辑图表，支持将多种格式（例如SVG, PNG）导出，便于进行视觉化展示。  
- **playwright-cli skill**: 用于自动化测试和浏览器操作，使得用户能够通过脚本执行各种浏览器操作，适合进行网页测试。

## 2. 集成场景说明  
在一个项目中，可能需要生成报告并包含图表及自动化测试结果。此时可以按需组合这些技能，以实现高效的工作流程，确保文档内容的自动化更新与转化。

## 3. 具体使用步骤  
1. 使用 **doc skill** 来生成基础文档，确定文档结构。
2. 应用 **drawio skill** 来创建所需的图表，保存为适当格式。
3. 使用 **playwright-cli skill** 进行自动化测试，将测试结果整合到文档中。
4. 最后，使用 **doc skill** 将所有内容合并成最终报告。

## 4. 工作流程示例  
- 生成的文档架构: 介绍 → 方法 → 结果 → 结论  
- 在结果部分，包含使用 **drawio skill** 生成的图表，展示关键数据。
- 使用 **playwright-cli skill** 的输出作为附录内容，以便于展示测试结果。

## 5. 常见问题解答  
- **如何将图表嵌入文档中？**  
  图表可以通过markdown格式或HTML代码嵌入，确保图片路径正确。
- **如何处理自动化测试错误？**  
  检查脚本和元素选择器是否正确，必要时增加错误处理和重试机制。
