# 使用指南

本指南按“真实项目证据 -> 提交版兼容章节结构 -> Word 终稿处理”的顺序说明如何使用本 Skill。

## 前置说明

`gzcu-thesis-spec` 的默认目标不是生成一份“看起来像论文”的草稿，而是尽量对齐广州城市理工学院已经验证可提交的论文组织和 Word 实现方式。

按任务类型选用其他 Skill：

- 如果只做规则解释、章节写作、常规文档整理，可以只使用本 Skill。
- 如果需要处理 `.docx` 排版、分节、页眉页脚、页码和最终 Word 检查，使用 `doc`。
- 如果需要正式图表，使用 `drawio`。
- 如果需要浏览器截图、页面证据或 UI 流程验证，使用 `playwright-cli`。
- 如果要运行 `build_reference_crossrefs.py`、`build_figure_crossrefs.py`、`build_table_crossrefs.py`，必须满足 `Windows + Microsoft Word + pywin32`。

最小 Python 依赖安装命令：

```bash
py -m pip install python-docx pywin32
```

## 1. 默认工作路径

推荐按下面的路径使用：

1. 先收集真实项目材料。
2. 先让 Skill 建立 `compliance matrix` 和 `evidence map`。
3. 按提交版兼容的 6 章结构逐章生成或修订内容。
4. 最后再做 Word 分节、页眉页码、目录和正文引用交叉引用。
5. 提交前再做一次合规审查。

默认兼容结构：

1. 第一章 绪论
2. 第二章 相关技术与理论基础
3. 第三章 系统需求分析
4. 第四章 系统总体设计
5. 第五章 系统详细设计与实现
6. 第六章 系统测试与部署

## 2. 提交硬要求与增强项

### 提交硬要求

- 封面和说明页保持模板原样。
- 从中文摘要页开始应用排版规则。
- Word 默认采用四段分节：
  - 封面 + 说明页
  - 中英文摘要
  - 目录
  - 正文至致谢
- 摘要使用 Roman 页码。
- 目录属于独立分节，但隐藏页码。
- 正文从 Chapter 1 开始 Arabic 页码 `1`。
- 每章、结论、参考文献、致谢都从奇数页开始。
- 偶数页页眉固定为 `广州城市理工学院本科毕业设计（论文）`。
- 奇数页页眉默认通过当前一级标题的 `STYLEREF` 结果带入。
- 目录由 Word 自动生成。
- 正文引用与参考文献顺序一致。
- 最终 `.docx` 的正文引用默认应支持点击跳转到参考文献。
- 正文引用跳转参考文献属于已验证的基线行为。

### 终稿增强项

- 图表正文引用做成可点击跳转。
- 图表素材批量插入和图块间距自动归一化。
- 用脚本批量补正文引用字段、图交叉引用、表交叉引用。

如果用户没有明确要求，图表点击跳转不作为基础合规门槛。

## 3. 推荐调用方式

### 3.1 先做证据盘点

```text
使用 $gzcu-thesis-spec，先不要写论文。请根据广州城市理工学院本科毕业论文要求和提交版模板，给我一份真实项目材料采集清单，并标出高优先级缺失项。
```

### 3.2 先做规则矩阵

```text
使用 $gzcu-thesis-spec，根据学校规则、提交版模板和我的项目材料，先建立论文写作的 compliance matrix 和 evidence map，再告诉我哪些章节可以先写。
```

### 3.3 写第二章相关技术与理论基础

```text
使用 $gzcu-thesis-spec，根据以下项目材料撰写第二章相关技术与理论基础，只保留与项目真实实现直接相关的技术基础，不要写成教材式堆砌。
```

### 3.4 写第六章系统测试与部署

```text
使用 $gzcu-thesis-spec，根据以下测试记录和部署材料撰写第六章系统测试与部署，必须包含测试环境、测试结果分析、部署过程与运行支撑，不得虚构数据。
```

### 3.5 检查 Word 终稿规则

```text
使用 $gzcu-thesis-spec 和 $doc，检查这篇论文是否符合四段分节、页眉页码、奇数页起章、目录和正文引用交叉引用要求。
```

### 3.6 处理正文引用跳转

```text
使用 $gzcu-thesis-spec，给我这篇 docx 的正文引用交叉引用处理方案，并说明如何把正文 [1] [2] 改成可点击跳转到参考文献。
```

## 4. 什么时候用脚本

### 默认建议运行

- `build_reference_crossrefs.py`
  - 用于把正文 `[1]`、`[2]` 之类的纯文本引用转成 Word 交叉引用。
  - 参考文献条目可按 `gzcu_ref_n` 命名书签。
  - 重建范围只进入正文主文，不进入摘要、本章小结、结论。

### 按需运行

- `assemble_thesis_assets.py`
  - 需要批量插图、截图、白底代码图时再用。
- `normalize_figure_paragraphs.py`
  - 需要统一图片段落和图块间距时再用。

### 用户明确要求才运行

- `build_figure_crossrefs.py`
- `build_table_crossrefs.py`

这两个脚本属于终稿增强，不默认视为提交必做项。

## 4.5 什么时候看 evals

如果你要检查这个 Skill 自己有没有“说偏”或“退回旧模板”，优先看仓库根目录的 `evals/`：

- `evals/cases/`
  - 放常见请求场景
- `evals/expected/`
  - 放每类请求必须命中的基线检查点
- `evals/checks/README.md`
  - 说明如何把这些 case 当成轻量回归使用

`evals/` 的作用不是检查真实 `.docx` XML 结构，而是检查这个 Skill 的默认回答、说明文档和审查口径有没有继续保持：

- 6 章默认结构
- 4 段 Word 分节
- 正文引用跳转参考文献属于基线
- 图表点击跳转默认属于按需增强
- `gzcu_ref_n` 与正文引用重建边界是否仍然清楚
- 目录隐藏页码、奇数页 `STYLEREF` 页眉是否仍然清楚

如果你检查的是一份真实 `docx` 是否符合提交版基线，仍应优先运行 `check_docx_baseline.py`。

## 5. 常见误区

- 不要只有一句项目简介就直接让 AI 写完整论文。
- 不要继续套用旧的“需求分析直接接在绪论后面”的固定模板。
- 不要把测试和部署都写成空泛说明，必须落到真实证据。
- 不要把摘要、本章小结、结论写上文献引用。
- 不要把正文引用留成最终纯文本编号。
- 不要把图表点击跳转和参考文献点击跳转混为一谈。
- 不要为了“目录看起来完整”先加一个职责模糊的顶层 `assets/` 目录。

## 6. 最终检查建议

提交前至少确认：

- 章节结构是否与提交版兼容。
- Word 分节是否正确。
- 奇数页起章是否正确。
- 页眉是否正确显示。
- 页码是否正确切换。
- 目录是否已更新。
- 正文引用是否已与参考文献建立跳转关系。
- 图表、表格、代码截图是否仍然符合学校风格。
