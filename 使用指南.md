# 使用指南

本指南只覆盖三类核心任务：

- 按学校规范写论文
- 按学校规范检查论文
- 按学校规范给出 Word 排版建议

不再默认扩展到截图取证、Draw.io 出图、资产装配、评测回归或交叉引用自动化。

## 1. 使用原则

- 先看学校 `附件4-1：毕业设计（论文）格式规范.doc`
- 再把提交版 `.docx` 当作参考基线
- 先输出“学校规则矩阵 + 当前论文适配点”
- 再写章节、审查问题或给 Word 建议

## 2. 何时写章节

当你已经提供项目材料、任务书范围或现有论文草稿时，可以让 Skill 写章节。

推荐调用方式：

```text
使用 $gzcu-thesis-spec，根据学校论文格式规范和以下项目材料，先给出规则矩阵与当前章节适配点，再撰写第一章绪论。
```

章节生成默认遵循这些约束：

- 中文摘要 `400-600` 字，关键词 `3-5` 个
- 正文使用规范的章节层级
- 不把提交版样本里的专业章节结构写成所有论文唯一模板
- 参考文献按正文首次出现顺序编号
- 结论与致谢按学校要求单独成章

## 3. 何时检查论文

当你已经有章节草稿或完整论文时，可以让 Skill 做合规检查。

推荐调用方式：

```text
使用 $gzcu-thesis-spec，按广州城市理工学院论文格式规范检查这篇论文。先给我学校规则矩阵，再列出当前不合规项和人工复核项。
```

检查时优先覆盖：

- 摘要和关键词
- 目录
- 标题层级
- 正文字体字号与行距
- 图题、表题和图表编号
- 参考文献格式与编号顺序
- 致谢格式
- 奇数页起章与页码切换

## 4. 何时给 Word 建议

当你要把内容整理成终稿时，可以让 Skill 给出 Word 排版建议。

推荐调用方式：

```text
使用 $gzcu-thesis-spec，按照学校格式规范和提交版样本的稳定做法，给我这篇论文的 Word 排版建议，并区分学校硬规则与样本参考实现。
```

Word 建议默认优先对齐：

- 正文小四宋体
- 固定值 `20 磅` 行距
- 段首缩进 `2` 个汉字
- 一级、二级、三级标题规则
- 图题下置、表题上置
- 图表按章编号
- 参考文献与致谢正文五号宋体
- 每章另起页，终稿阶段关注奇数页起章

## 5. 关于提交版样本

提交版 `.docx` 只用于提供更具体的默认建议，常见可参考点包括：

- `4` 段分节
- 软件工程论文常见 `6` 章结构
- 摘要 Roman 页码、正文 Arabic 页码
- 奇偶页眉机制

这些都应表述为“参考基线”，不能替代学校 `.doc` 的硬规则地位。

## 6. 关于基线检查脚本

如果你已经有真实 `.docx`，可以结合 `gzcu-thesis-spec/scripts/check_docx_baseline.py` 做结构核对。

它的定位是：

- 辅助比较当前文档与提交版样本是否接近
- 帮助发现分节、页码、页眉等结构问题

它不是学校规则全文，也不是 Skill 的默认输出主线。
